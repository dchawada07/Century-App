{
  "name": "transportos-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Dependency-free localhost demo for TransportOS Phase 0 workflows.",
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "build": "node scripts/validate-app.mjs"
  }
}
